[project]
name = "photo"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pelican[markdown]>=4.11.0.post0",
    "pillow>=12.1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.14",
]
